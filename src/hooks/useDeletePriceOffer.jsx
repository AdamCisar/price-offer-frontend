const useDeletePriceOffer = () => {

    const deletePriceOffer = (idList) => {
        console.log(idList);
    };
   
    return {
        deletePriceOffer
    };
};

export default useDeletePriceOffer;
